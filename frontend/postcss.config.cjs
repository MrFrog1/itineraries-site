// Use CommonJS syntax for CommonJS file
const tailwindcss = require('tailwindcss');

module.exports = {
    plugins: [
        tailwindcss,
        // Add other PostCSS plugins here if needed
    ],
};