import { combineReducers } from 'redux';
// import bookings from './bookings';
// import userReducer from './userSelect';
// import errors from './errors';


const rootReducer = combineReducers({
    // bookings,
    // userReducer,
    // errors  //we can just assign names to the existing reducers to simplify it, with a colon. ie userSelect: userReducer

});

export default rootReducer